services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile.monolith
    image: movie-app:monolith
    container_name: movie-app-monolith
    ports:
      - "8000:8000"
    restart: unless-stopped
    mem_limit: 512m
    cpus: 1.0
